{
  "name": "framework-skeleton",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cucumber-js --require src/support/world.cjs --require src/support/hooks.cjs --require \"src/steps/**/*.cjs\" --format progress",
    "clean": "rimraf reports test-results .playwright",
    "test:json": "npx cucumber-js --require src/support/world.cjs --require src/support/hooks.cjs --require \"src/steps/**/*.cjs\" --format json:reports/json/cucumber.json",
    "report": "node reporter.js",
    "test:report": "npm run clean && npm run test:json && npm run report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "dotenv": "^17.2.2",
    "multiple-cucumber-html-reporter": "^3.9.3",
    "playwright": "^1.55.0",
    "rimraf": "^6.0.1"
  }
}
